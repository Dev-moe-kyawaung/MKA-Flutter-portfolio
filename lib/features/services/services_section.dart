import 'package:flutter/material.dart';
import 'package:provider/provider.dart';import '../../data/state/portfolio_provider.dart';import '../home/widgets/section_container.dart';import '../home/widgets/section_title.dart';import 'widgets/service_card.dart';class ServicesSection extends StatelessWidget {  const ServicesSection({super.key});  @override  Widget build(BuildContext context) {    return SectionContainer(      child: Consumer<PortfolioProvider>(        builder: (context, vm, _) {          return Column(            crossAxisAlignment: CrossAxisAlignment.start,            children: [              const SectionTitle(                title: 'Services',                subtitle: 'What I can help you build.',              ),              const SizedBox(height: 20),              Wrap(                spacing: 16,                runSpacing: 16,                children: vm.services                    .map((s) => ServiceCard(title: s.title, description: s.description))                    .toList(),              ),            ],          );        },      ),    );  }}