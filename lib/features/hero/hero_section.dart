import 'package:flutter/material.dart';
import 'package:provider/provider.dart';import '../../app/theme/app_colors.dart';import '../../core/constants/app_links.dart';import '../../core/services/launch_service.dart';import '../../data/state/portfolio_provider.dart';import '../home/widgets/section_container.dart';import 'widgets/hero_cta_buttons.dart';class HeroSection extends StatelessWidget {  const HeroSection({super.key});  @override  Widget build(BuildContext context) {    return SectionContainer(      child: Consumer<PortfolioProvider>(        builder: (context, vm, _) {          return Container(            padding: const EdgeInsets.all(32),            decoration: BoxDecoration(              color: AppColors.surface,              borderRadius: BorderRadius.circular(28),              border: Border.all(color: AppColors.border),            ),            child: Column(              crossAxisAlignment: CrossAxisAlignment.start,              children: [                Text(                  'Hi, I’m ${vm.profile.name}',                  style: const TextStyle(fontSize: 52, fontWeight: FontWeight.w800),                ),                const SizedBox(height: 14),                Text(                  '${vm.profile.title} • Flutter • Kotlin • Jetpack Compose',                  style: const TextStyle(fontSize: 20, color: AppColors.mutedText),                ),                const SizedBox(height: 22),                const Text(                  'I build premium, responsive, and performance-focused applications for Android and web.',                  style: TextStyle(fontSize: 18, height: 1.7),                ),                const SizedBox(height: 28),                Wrap(                  spacing: 12,                  runSpacing: 12,                  children: [                    FilledButton(                      onPressed: () => LaunchService.open(vm.profile.github),                      child: const Text('GitHub'),                    ),                    OutlinedButton(                      onPressed: () => LaunchService.open(AppLinks.email),                      child: const Text('Contact Me'),                    ),                  ],                ),              ],            ),          );        },      ),    );  }}