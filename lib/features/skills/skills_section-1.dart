import 'package:flutter/material.dart';
import 'package:provider/provider.dart';import '../home/widgets/section_container.dart';import '../home/widgets/section_title.dart';import '../../data/state/portfolio_provider.dart';import 'widgets/skill_chip.dart';class SkillsSection extends StatelessWidget {  const SkillsSection({super.key});  @override  Widget build(BuildContext context) {    return SectionContainer(      child: Consumer<PortfolioProvider>(        builder: (context, vm, _) {          return Column(            crossAxisAlignment: CrossAxisAlignment.start,            children: [              const SectionTitle(                title: 'Skills',                subtitle: 'Technologies and tools I use to build production-ready apps.',              ),              const SizedBox(height: 20),              Wrap(                spacing: 12,                runSpacing: 12,                children: vm.skills.map((e) => SkillChip(label: e)).toList(),              ),            ],          );        },      ),    );  }}