import 'package:flutter/material.dart';
import 'package:provider/provider.dart';import '../../data/state/portfolio_provider.dart';import '../home/widgets/section_container.dart';import '../home/widgets/section_title.dart';import 'widgets/project_card.dart';class ProjectsSection extends StatelessWidget {  const ProjectsSection({super.key});  @override  Widget build(BuildContext context) {    return SectionContainer(      child: Consumer<PortfolioProvider>(        builder: (context, vm, _) {          return Column(            crossAxisAlignment: CrossAxisAlignment.start,            children: [              const SectionTitle(                title: 'Projects',                subtitle: 'Selected work and highlights.',              ),              const SizedBox(height: 20),              Wrap(                spacing: 16,                runSpacing: 16,                children: vm.projects                    .map(                      (p) => ProjectCard(                        title: p.title,                        description: p.description,                        imageUrl: p.imageUrl,                        tags: p.tags,                      ),                    )                    .toList(),              ),            ],          );        },      ),    );  }}